export type TableProps = {
    children: React.ReactNode;
    border?: string;
    width?: string;
  };
  
  export type TableCellProps = {
    children: React.ReactNode;
    align?: 'left' | 'center' | 'right';
  };